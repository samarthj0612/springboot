server.port=8080

spring.datasource.url=jdbc:h2:mem:devdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=samdb
spring.datasource.password=samjain
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true

# logging.level.org.springframework=DEBUG

openapi.url=http://localhost:8080

# To support live reload.
spring.devtools.restart.enabled=true

# Configuration for fake SMTP server (MailHog)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# To enable more detailed logging for the mail-related properties
# logging.level.org.springframework.mail=DEBUG
# logging.level.org.springframework.boot.autoconfigure.mail=TRACE
